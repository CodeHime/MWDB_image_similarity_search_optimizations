# MWDB Image Similarity Search Optimizations

## Setup
If you have anaconda setup then run:
```python
conda env create -f environment.yml
```
else manually install all the libraries listed in the environment.yml file.
